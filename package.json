{
  "name": "natours-api",
  "module": "app.ts",
  "type": "module",
  "scripts": {
    "dev": "bun --hot app.ts",
    "build": "bun build --compile --minify --sourcemap ./app.ts --outfile myapp"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/express": "^5.0.0",
    "@types/morgan": "^1.9.9",
    "@types/validator": "^13.12.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "express": "^4.21.2",
    "mongoose": "^8.10.1",
    "morgan": "^1.10.0",
    "slugify": "^1.6.6",
    "validator": "^13.12.0"
  }
}
